"""TraceLit — API v2 Top-Level Router (Phase 3 stubs)."""

from fastapi import APIRouter

router = APIRouter()

# v2 sub-routers will be registered here in Phase 3
# from api.v2.papers.router import router as papers_router
# router.include_router(papers_router, tags=["v2/papers"])
